/** @type {import('next').NextConfig} */
const nextConfig = {
   
        output: 'export', // This allows you to export your app as a static site
        // Add other configurations as necessary
    
      
      
};

export default nextConfig;
